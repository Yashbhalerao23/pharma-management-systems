modules = ["python-3.11", "postgresql-16"]

[nix]
channel = "stable-24_05"
packages = ["freetype", "lcms2", "libimagequant", "libjpeg", "libtiff", "libwebp", "libxcrypt", "openjpeg", "tcl", "tk", "zlib"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Pharmacy Management System"

[[workflows.workflow]]
name = "Pharmacy Management System"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd pharmamgmt && python manage.py migrate --run-syncdb && python manage.py runserver 0.0.0.0:5000"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "cd pharmamgmt && pip install django Pillow && python manage.py makemigrations && python manage.py migrate && python manage.py runserver 0.0.0.0:5000"]

[[ports]]
localPort = 5000
externalPort = 80
